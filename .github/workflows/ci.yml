# **NOTE**: this file is generated by `dhall-render`.
# You should NOT edit it manually, your changes will be lost.

---
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: '1'
    - env:
        DOCKER_PASSWORD: "${{ secrets.GITHUB_TOKEN }}"
      name: Login to docker.pkg.github.com
      run: |-
        set -eu -o pipefail
        set -x
        docker login -u "$GITHUB_ACTOR" --password-stdin docker.pkg.github.com <<EOF
        $DOCKER_PASSWORD
        EOF
    - env:
        BRANCH_REF: "${{ github.head_ref || github.ref }}"
      name: Docker build
      run: |-
        set -eu -o pipefail
        set -x
        docker pull "docker.pkg.github.com/timbertson/auditspec/app:latest" || true
        docker pull "docker.pkg.github.com/timbertson/auditspec/app:$(echo "${BRANCH_REF#refs/heads/}" | tr / -)" || true
        docker "build" "--progress=plain" "--file=Dockerfile" "--cache-from=docker.pkg.github.com/timbertson/auditspec/app:latest" "--cache-from=docker.pkg.github.com/timbertson/auditspec/app:$(echo "${BRANCH_REF#refs/heads/}" | tr / -)" "--tag=docker.pkg.github.com/timbertson/auditspec/app:$GITHUB_SHA" "--tag=docker.pkg.github.com/timbertson/auditspec/app:$(echo "${BRANCH_REF#refs/heads/}" | tr / -)" "."
        docker push "docker.pkg.github.com/timbertson/auditspec/app:$GITHUB_SHA"
        docker push "docker.pkg.github.com/timbertson/auditspec/app:$(echo "${BRANCH_REF#refs/heads/}" | tr / -)"
    - if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref
        == 'refs/heads/master')
      name: Docker push :latest
      run: |-
        set -eu -o pipefail
        set -x
        docker tag "docker.pkg.github.com/timbertson/auditspec/app:$GITHUB_SHA" "docker.pkg.github.com/timbertson/auditspec/app:latest"
        docker push "docker.pkg.github.com/timbertson/auditspec/app:latest"
    - name: Check generated files
      run: |-
        set -eu -o pipefail
        set -x
        docker run --rm -i "docker.pkg.github.com/timbertson/auditspec/app:$GITHUB_SHA" bash -eu -o pipefail <<EOF_runInDocker
          sbt 'strict compile' test

        EOF_runInDocker
name: CI
'on':
  pull_request: {}
  push:
    branches:
    - master
    - main
